{
  "name": "cat-gifs",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "antd": "^3.0.0",
    "bs-fetch": "^0.2.0",
    "photon-ant": "^0.1.2",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "reason-scripts": "0.8.0"
  },
  "scripts": {
    "start": "react-app-rewired start --scripts-version reason-scripts",
    "build": "react-app-rewired build --scripts-version reason-scripts",
    "test": "react-sapp-rewired test --env=jsdom --scripts-version reason-scripts",
    "eject": "react-app-rewired eject --scripts-version reason-scripts",
    "prepare": "npm link bs-platform"
  },
  "devDependencies": {
    "babel-plugin-import": "^1.6.2",
    "bs-jest": "^0.2.0",
    "react-app-rewire-less": "^2.1.0",
    "react-app-rewired": "^1.3.5",
    "reason-react": "^0.3.0"
  }
}
